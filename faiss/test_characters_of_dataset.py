import numpy as np 

dataset_list = [


]

query_list = [


]

learn_list = [


]

query_k_list = [1, 5, 10, 20, 40, 80, 120, 200, 400, 600, 800, 1000]

for i in range(dataset_list):
    
    for dataset
    #parameter for IVFflat
    IVFFlat_list = [[], []]
    for k in query_k_list:
        for parameter in IVFFlat_list:
            time_1 = time.time()
            recall_record = np.zeros()


    #parameter for IVFPQ
    IVFPQ_list = [[], []]

    #parameter for HNSW
    HNSW_list = [[], []]

    #parameter for LSH
    LSH_LIST = [[], []]

    #parameter for PQ
    PQ_list = [[], []]